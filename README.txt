1. run the code and you will find a different window where you can choose any jpg image, it will warn you if you choose different file format. you can choose the directory and this will give you a txt file output. where you can find the text those were in your image.

2. you can change the size inside the code so that the scannnig  rectangle through which it pass on image changes and the comparison it does with the imported data inside the tesseract-ocr folder will be done accordingly (make size bigger if photo have big size text and make size smaller if photo have small texts)

3. make sure you put the code and the tesseract-ocr folder in same directory
4. have enjogy !


This is specially helpful for the blind people and all !
